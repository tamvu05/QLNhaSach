<div class="container py-5">
    <div class="text-center mb-5">
        <h2 class="fw-bold text-primary">📚 Chào mừng bạn đã đến với kệ sách của chúng tôi</h2>
        <p class="text-muted">"Đừng chỉ sống một cuộc đời, hãy sống ngàn cuộc đời qua những trang sách."</p>
    </div>

    <% if (data && data.length > 0) { %>
        <div class="row row-cols-1 row-cols-md-3 row-cols-lg-4 g-4">
            <% data.forEach(function(book) { %>
                <div class="col">
                    <div class="card h-100 shadow-sm border-0 book-card-hover">
                        
                        <div class="position-relative overflow-hidden" style="height: 300px; background-color: #f8f9fa;">
                            <% if (book.HinhAnh) { %>
                                <img src="<%= book.HinhAnh %>" class="card-img-top w-100 h-100" style="object-fit: contain; padding: 10px;" alt="<%= book.TenSach %>">
                            <% } else { %>
                                <div class="d-flex align-items-center justify-content-center w-100 h-100 text-muted">
                                    <span>Chưa có ảnh</span>
                                </div>
                            <% } %>
                            
                            <span class="position-absolute top-0 end-0 badge bg-danger m-2">-10%</span>
                        </div>

                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title text-truncate fw-bold" title="<%= book.TenSach %>">
                                <%= book.TenSach %>
                            </h5>
                            
                            <p class="card-text text-muted small mb-2">
                                ✏️ <%= book.TacGia %>
                            </p>

                            <div class="mt-auto d-flex justify-content-between align-items-center">
                                <span class="h5 text-primary fw-bold mb-0">
                                    <%= new Intl.NumberFormat('vi-VN', { style: 'currency', currency: 'VND' }).format(book.DonGia || 0) %>
                                </span>
                            </div>
                        </div>

                        <div class="card-footer bg-white border-0 pb-3">
                            <div class="d-grid gap-2">
                                <a href="/book/<%= book.id %>" class="btn btn-outline-primary">
                                    Xem chi tiết
                                </a>
                                <button class="btn btn-primary">
                                    🛒 Thêm vào giỏ
                                </button>
                            </div>
                        </div>

                    </div>
                </div>
            <% }); %>
        </div>

    <% } else { %>
        <div class="alert alert-warning text-center py-5" role="alert">
            <h4 class="alert-heading">Kho sách đang trống!</h4>
        </div>
    <% } %>

    <% if (totalPages > 1) { %>
        <nav aria-label="Page navigation" class="mt-5">
            <ul class="pagination justify-content-center">
                
                <li class="page-item <%= currentPage === 1 ? 'disabled' : '' %>">
                    <a class="page-link" href="/book?page=<%= currentPage - 1 %>">Trước</a>
                </li>

                <% for(let i = 1; i <= totalPages; i++) { %>
                    <li class="page-item <%= currentPage === i ? 'active' : '' %>">
                        <a class="page-link" href="/book?page=<%= i %>"><%= i %></a>
                    </li>
                <% } %>

                <li class="page-item <%= currentPage === totalPages ? 'disabled' : '' %>">
                    <a class="page-link" href="/book?page=<%= currentPage + 1 %>">Sau</a>
                </li>
            </ul>
        </nav>
    <% } %>
</div>

<style>
    .book-card-hover:hover {
        transform: translateY(-5px);
        transition: all 0.3s ease;
        box-shadow: 0 10px 20px rgba(0,0,0,0.1) !important;
    }
</style>