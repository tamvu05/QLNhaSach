<nav class="navbar navbar-expand-lg navbar-dark bg-primary sticky-top shadow-sm">
    <div class="container">
        <a class="navbar-brand fw-bold" href="/">📚 BookStore</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link <%= path === '/' ? 'active' : '' %>" href="/">Trang chủ</a>
                </li>
                
                <li class="nav-item">
                    <a class="nav-link <%= path === '/book' ? 'active' : '' %>" href="/book">Sách</a>
                </li>
                
                <li class="nav-item">
                    <a class="nav-link <%= path === '/category' ? 'active' : '' %>" href="/category">Thể loại</a>
                </li>
            </ul>
            
            <form class="d-flex me-3" role="search" action="/book" method="GET">
                <input class="form-control me-2" type="search" name="keyword" placeholder="Nhập tên sách..." aria-label="Search" value="<%= typeof keyword !== 'undefined' ? keyword : '' %>">
                <button class="btn btn-outline-light" type="submit">Tìm</button>
            </form>

            <div class="d-flex gap-2">
                <a href="#" class="btn btn-light text-primary">Đăng nhập</a>
                <a href="#" class="btn btn-warning">Giỏ hàng (0)</a>
            </div>
        </div>
    </div>
</nav>